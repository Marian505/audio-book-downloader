# audio-book-downloader
how to run:

prerequisite:
python, pip, uv

install uv:
pip install uv

load dependecies:
uv sync

run script:
uv run ./main.py

if dependencies are aleready instaled in your python env, should be enought 
python ./main.py

customize script to your needs and set: search_phrase, url
